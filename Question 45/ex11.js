/* 11.Store the names of a few of your friends in a array called names.
Print each person’s name by accessing each element in the list, one at a time.*/
var names = ["Ahmed", "Usman", "Amir", "Hamza", "Ahsan", "Fiaz"];
for (var i = 0; i < names.length; i++) {
    console.log(names[i]);
}
